export default function Inquiries({ inquiries, manager, onSubmit }) {
  return <div className="content"><div className="section-heading"><div><p className="eyebrow">CONVERSATIONS</p><h2>{manager ? 'Listen, then act.' : 'Speak with your manager.'}</h2></div></div><div className="inquiry-layout"><section className="panel inquiry-list">{inquiries.map((item) => <div className="inquiry-card" key={item.id}><div><span className="pill open">{item.status}</span><h3>{item.subject}</h3><p>{item.message}</p></div><small>{item.tenantName}<br />{item.date}</small></div>)}</section>{!manager && <form className="panel inquiry-form" onSubmit={onSubmit}><p className="eyebrow">NEW MESSAGE</p><h3>Send an inquiry</h3><input required name="subject" placeholder="Subject" /><textarea required name="message" placeholder="Tell us what is happening..." /><button className="primary">Send inquiry ↗</button></form>}</div></div>
}
