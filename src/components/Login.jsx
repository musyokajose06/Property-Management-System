export default function Login({ onManager, onAdmin, onTenant, tenants }) {
  return <main className="login-page"><div className="login-panel"><div className="brand"><b>R</b><strong>R3NT<span>LEDGER</span></strong></div><p className="eyebrow">PROPERTY OPERATIONS, REFINED</p><h1>Everything under one roof.</h1><p className="login-copy">A calm, clear workspace for the people who manage homes and the people who live in them.</p><button className="primary" onClick={onManager}>Enter manager workspace →</button><button className="secondary" onClick={onAdmin}>Preview admin access</button><select defaultValue="" onChange={(event) => event.target.value && onTenant(event.target.value)}><option value="">Enter as a tenant...</option>{tenants.map((tenant) => <option key={tenant.id} value={tenant.id}>{tenant.name} · unit {tenant.unit}</option>)}</select><small className="demo-note">Demo environment · data is stored locally in your browser</small></div><div className="login-art"><div><span>01</span><strong>A better rhythm<br />for every resident.</strong></div></div></main>
}
